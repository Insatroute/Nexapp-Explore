---
title: Sla
full: true
_openapi:
  preload:
    - ./controller-openapi.docs.json
  toc:
    - depth: 2
      title: Sla_device_sla_list
      url: '#sla_device_sla_list'
    - depth: 2
      title: Assign an existing SLAMetric to a specific device.
      url: '#assign-an-existing-slametric-to-a-specific-device'
    - depth: 2
      title: Remove the device-scoped SLA from a device.
      url: '#remove-the-device-scoped-sla-from-a-device'
  structuredData:
    headings:
      - content: Sla_device_sla_list
        id: sla_device_sla_list
      - content: Assign an existing SLAMetric to a specific device.
        id: assign-an-existing-slametric-to-a-specific-device
      - content: Remove the device-scoped SLA from a device.
        id: remove-the-device-scoped-sla-from-a-device
    contents:
      - content: |-
          Clones the picked SLAMetric with scope_type='device' + device=<this>.
          The original SLA row is NEVER modified — other devices inheriting it
          via org/group scope continue to work unchanged. If the device already
          has a device-scoped SLA, it is replaced (deleted) with the new clone.
        heading: assign-an-existing-slametric-to-a-specific-device
      - content: >-
          Deletes the `scope_type='device'` SLAMetric row(s) for this device.

          Group / organization-scoped SLAs are untouched — if any exist they

          take effect via the normal fallback (see
          services.get_effective_sla_for_device).

          If no device-scoped SLA exists, returns success with deleted=0.
        heading: remove-the-device-scoped-sla-from-a-device
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./controller-openapi.docs.json" webhooks={[]} operations={[{"path":"/sla/device/{id}/sla/","method":"get"},{"path":"/sla/device/{id}/sla/assign/","method":"post"},{"path":"/sla/device/{id}/sla/unassign/","method":"post"}]} showTitle showDescription />
    </>
  );
}