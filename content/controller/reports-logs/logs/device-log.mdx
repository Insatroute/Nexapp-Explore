---
title: "Device log"
description: "Reports & Logs › Logs › Device log"
---

<Cards>
  <Card title="Path" description="/logs/device" />
  <Card title="Who can open it" description="Requires any permission on device_logs.devicelog." />
</Cards>

Syslog from the devices themselves. Unlike the other three logs this is not a database table — the view proxies Graylog and parses each line, so what you can search here is bounded by what Graylog holds rather than by the controller's own database.

## What you can do here

- **Browse and search device log** — Syslog from the devices. Unlike the other three logs this is not a database table — the view proxies Graylog and parses each line, so what you can search is bounded by what Graylog holds.
- **Browse and search device log hostnames** — The hostnames Graylog has seen, which is what the device filter on this page offers.

---

<small>Read from: component `DeviceLog`; description from `pages/DeviceLog.jsx — its header comment: "Syslog from the devices themselves. Unlike the other three this is not a database table — the view proxies Graylog, parses each line…"`.</small>

