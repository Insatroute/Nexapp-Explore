---
title: "Firmware"
description: "Firmware"
---

<Cards>
  <Card title="Path" description="/firmware" />
  <Card title="Who can open it" description="Requires any permission on firmware_upgrader.build." />
</Cards>

Firmware in three parts: the CATEGORIES that group images, the BUILDS themselves with their version and OS identifier, and the UPGRADE batches that push a build to devices and report how far each one got.

A build belongs to a category and targets an OS identifier, which is what stops an image being offered to hardware it does not fit.

## Why it sits here

No heading. The hub carries builds, categories and mass upgrades as its own tabs, so "Services" was a section wrapping exactly one destination — an extra click and an extra word for nothing.

<Callout type="info">Authored note, from the `NAV` array in `Sidebar.jsx`.</Callout>

## What you can do here

### Actions

- **Delete firewall build** — Removes an image. Devices already running it are unaffected — this withdraws it from future upgrades.
- **Delete firewall category** — Removes a category. Check the builds filed under it first; a category is how they are found.

### Reading and lookups

- **Browse and search batch ops** — Upgrade batches: which devices a build was pushed to and how far each one got.
- **Browse and search firewall builds** — The images themselves, each with a version and an OS identifier — the identifier is what stops a build being offered to hardware it does not fit.
- **Browse and search firewall categories** — The categories images are grouped into. (The call is named for the app it lives in; the page is firmware, not firewall.)
- **Browse and search organizations** — The organization list — what the scope picker narrows the page by, and what fills the Organization field on its forms. Almost every record in the controller is organization-scoped, which is why this call appears on so many pages.

## What the page shows

**Tabs** — `Builds` · `Categories` · `Mass upgrades`

---

<small>Read from: component `FirmwareHub`; description from `pages/FirmwareHub.jsx — headers across its three tables: Category, Name, Description, Organization, Type; Build, Version, OS identifier, Created; Devices, Status, Started`.</small>

