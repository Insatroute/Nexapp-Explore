---
title: "Users"
description: "Administration › Users & Organizations › Users"
---

<Cards>
  <Card title="Path" description="/users" />
  <Card title="Who can open it" description="Requires any permission on nexapp_users.user." />
</Cards>

Who can sign in, and what they are. Each row carries the user's role tier, the organizations they belong to, whether the account is active, whether two-factor is enabled and whether it is required of them.

Users sit beside Organizations because they are one subject split in half: who exists, and who they belong to.

## What you can do here

### Actions

- **Delete user** — Removes an account (`DELETE users/user/<id>/`). Deactivating instead keeps the audit trail intact — the access and activity logs reference the user, and a deleted one leaves rows attributed to nobody.

### Reading and lookups

- **Browse and search organizations** — The organization list — what the scope picker narrows the page by, and what fills the Organization field on its forms. Almost every record in the controller is organization-scoped, which is why this call appears on so many pages.
- **Browse and search users** — The accounts, from `users/user/` — with the role tier, the organizations each belongs to, whether the account is active, and the two-factor state.

---

<small>Read from: component `UserList`; description from `pages/UserList.jsx — headers User, Role, Organizations, Active, 2FA, 2FA req., Joined; role tier from UserList.roleOf (Superuser / Staff / User)`.</small>

