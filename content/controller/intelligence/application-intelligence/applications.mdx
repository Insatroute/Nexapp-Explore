---
title: "Applications"
description: "Intelligence › Application Intelligence › Applications"
---

<Cards>
  <Card title="Path" description="/app-intelligence/applications" />
  <Card title="Who can open it" description="Requires any permission on traffic_application.application." />
</Cards>

The applications the DPI engine recognises. Unlike its three sibling tabs this one is not a generic resource table — its descriptor is marked `custom`, so it is rendered by a component of its own rather than from a column/field list.

<Callout type="info">Served by the shared `AppIntelHub` component via the `/app-intelligence/:tab` route. Facts below describe that component, which renders several tabs.</Callout>

## What you can do here

### Actions

- **Delete resource** — Removes one.
- **Edit resource** — Changes an application entry.

### Reading and lookups

- **Browse and search resource** — The applications the DPI engine recognises. This tab is marked `custom` in its descriptor, so unlike its siblings it is drawn by a component of its own rather than from a column list.

**Also calls:** `appDetail` — listed verbatim because the name does not match a known verb.

**Opens:** `AppDatabase` · `ResourceTable` — some actions above are performed in these.

---

<small>Read from: component `AppIntelHub`; description from `pages/appintel/models.js APPLICATIONS_MODEL — { key: 'applications', label: 'Applications', icon: 'chart', custom: true }, with no resource or columns of its own`.</small>

