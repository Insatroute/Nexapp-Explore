---
title: "PIM"
description: "Policy Engine › PIM"
---

<Cards>
  <Card title="Path" description="/policy-engine/pim" />
  <Card title="Who can open it" description="Requires any permission on sdwan_tunnel.pimconfig." />
</Cards>

PIM multicast routing. The smallest policy here — it switches the protocol on for the devices in scope.

Scope is the same across the Policy Engine: a policy with **Apply fleet-wide** set reaches every device, otherwise it is narrowed to an organization. There is deliberately no device picker — configuring a single router is what that router's own CPE page is for.

<Callout type="info">Served by the shared `PolicyEngine` component via the `/policy-engine/:tab` route. Facts below describe that component, which renders several tabs.</Callout>

## What you can do here

### Actions

- **Delete resource** — Removes a policy via `DELETE /api/v1/nsbond/pim-config/<id>/`.
- **Edit resource** — Creates and edits a policy through the shared resource form. Fields: Name, Enabled.

### Reading and lookups

- **Browse and search resource** — Lists policies from `GET /api/v1/nsbond/pim-config/`, with columns Name, Scope, State.

**Opens:** `ResourceTable` — some actions above are performed in it.

---

<small>Read from: component `PolicyEngine`; description from `pages/policy/tabs.js descriptor for key 'pim': resource 'nsbond/pim-config', noun 'policy', and its columns/fields arrays (field names read from each endpoint's OPTIONS response, per the file's own note)`.</small>

